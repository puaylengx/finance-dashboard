# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9    # required for OAuth2PasswordRequestForm (form data)

# Validation & settings
pydantic>=2.9.0
pydantic-settings>=2.6.0

# PostgreSQL — async with connection pool (psycopg3)
psycopg[binary,pool]>=3.2.0

# SSH tunnel
sshtunnel>=0.4.0
paramiko<4.0
PyNaCl>=1.6.0

# Redis cache
redis[asyncio]>=5.2.0

# Auth & security
python-jose[cryptography]>=3.3.0
msal>=1.31.0
cryptography>=43.0.0
bcrypt>=5.0.0

# Rate limiting
slowapi>=0.1.9

# Logging
colorlog>=6.8.0
# Sentry (optional — ติดตั้งเพิ่มเติมถ้าต้องการ): pip install sentry-sdk[fastapi]>=2.0.0

# HTTP client (for Entra token validation)
httpx>=0.27.0

# Env config
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
